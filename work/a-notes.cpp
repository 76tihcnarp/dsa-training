//v.insert(v.begin()+index , val)

//     v.erase(v.begin()+ freq);

//  for (auto i : v2) cout << i;

// count:
//count(arr,arr + length, element);

//location of element:
// find(arr,arr + length, element) - arr;

//array equality
// bool ans = equal( arr1,arr+ len, arr2);

//reverse array:
//reverse(nums, nums+len);

//for each
// for (int num: arr_name){
// cout<<num<<" ";}

//vector declarations
// vector <int> v;
// vector <int> v (9);
// vector <int> v ={1,25,4,5,6,7};

//v.push_back(8);

//swapping values:
// swap(v1[2],v1[8]);

//max and min   [element]
//   auto ma= max_element(v.begin(),v.end());
//   auto mi= min_element(v.begin(),v.end());

// their location:
//    auto min_idx= find( v.begin(),v.end(),mi) -v.begin();
//    auto max_idx = find(v.begin(),v.end(),ma) -v.begin();

//accumulate
//accumulate(InputIterator first, InputIterator last, T init);

// array sort
//sort(arr_name,arr_name + length , comp_func)

//ascending:
//sort(arr_name,arr_name + length , less<int>());

//descending:
//sort(arr_name,arr_name + length , greater<int>());

// counting in vector;
// int freq = count( vec1.begin(),vec1.end(),element);

//removing element
// v.erase( v.begin() + index) ;

//swap
//  swap(v1[i+j],v1[i+j+k]);


//inserting element at specific index
//v.insert(v.begin()+ index , val);

// insert 1 vector into another:
//v1.insert(v1.end(),v2.begin(),v2end());

//location of element:
// int idx= find( v.begin(),v.end(),element); -v.begin();